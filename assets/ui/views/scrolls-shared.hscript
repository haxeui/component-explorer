function setWidth() {
    defaultHScroll.width = widthValue.pos;
    styledHScroll.width = widthValue.pos;
    bitmapHScroll.width = widthValue.pos;
}

function setHeight() {
    defaultHScroll.height = heightValue.pos;
    styledHScroll.height = heightValue.pos;
    bitmapHScroll.height = heightValue.pos;
}