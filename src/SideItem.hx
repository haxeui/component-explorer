package;

import haxe.ui.core.ItemRenderer;

@:build(haxe.ui.macros.ComponentMacros.build("assets/side-item.xml"))
class SideItem extends ItemRenderer {
    public function new() {
        super();
    }
}