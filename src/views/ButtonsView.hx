package views;

import haxe.ui.containers.Box;

@:build(haxe.ui.macros.ComponentMacros.build("assets/views/buttons.xml"))
class ButtonsView extends View {
    public function new() {
        super();
    }
}